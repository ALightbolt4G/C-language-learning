#include <stdio.h>
#include <math.h> // to get math functions

int main()
{
    float x = 3.14;
    
  //  x = sqrt(x); // square root function
  //  x = pow(x, 2); // power function pow(x, n) = x^n
  //  x = round(x); // rounding function
  //  x = ceil(x); // ceiling function
  //  x = floor(x); // floor function
  //  x = abs(x); // absolute function
  //  x = log(x); // logarithm function
  //  x = sin(x);  x = cos(x);  x = tan(x); // trigonometry functions
  
  
    
    printf("%f", x);
    
    
    return 0;
}