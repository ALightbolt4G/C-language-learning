#include <stdio.h>

int main() {
    
    // arthmetic operators = + - * / % ++ --
    
    float x = 10;
    float y = 3;
    float z = 0;
    
    z = x + y;
    //z = x - y;
    //z = x * y;
    //z = x / y;
    //z = x % y;  => remainder after devine numbers it give you the remain number
    
    // x++; => x+1;
    // x--; => x-1;
    
    // x+=2; => x + 2
    // x-=2; => x - 2 
    
    // x*=2; => x * 2
    // x/=2 => x / 2
    
    printf("%f", z);
    
    
    return 0;
}